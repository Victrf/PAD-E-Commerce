CREATE TABLE admins (
    id SERIAL PRIMARY KEY,
    email VARCHAR(100) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    price NUMERIC(10,2) NOT NULL,
    description TEXT,
    image_url TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE custom_orders (
    id SERIAL PRIMARY KEY,
    bag_description TEXT NOT NULL,
    contact_method VARCHAR(50) NOT NULL,
    contact_info VARCHAR(255) NOT NULL,
    file_paths TEXT[],
    created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE contact_forms (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL,
    phone VARCHAR(50),
    subject VARCHAR(255),
    message TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);






ALTER TABLE custom_orders ADD COLUMN IF NOT EXISTS processed BOOLEAN DEFAULT FALSE;
ALTER TABLE contact_forms ADD COLUMN IF NOT EXISTS processed BOOLEAN DEFAULT FALSE;


ALTER TABLE custom_orders ADD COLUMN IF NOT EXISTS processed boolean DEFAULT false;
ALTER TABLE contact_forms ADD COLUMN IF NOT EXISTS processed boolean DEFAULT false;



ALTER TABLE custom_orders ADD COLUMN processed BOOLEAN DEFAULT FALSE;
ALTER TABLE contact_forms ADD COLUMN processed BOOLEAN DEFAULT FALSE;


ALTER TABLE products
ADD COLUMN side_view VARCHAR(255),
ADD COLUMN back_view VARCHAR(255);


